[package]
name = "lazytmux"
version = "0.1.3"
edition = "2024"
authors = ["Amjad Hossain <amjad.jibon@gmail.com>"]
description = "A keyboard-first visual workspace explorer for tmux built in Rust with Ratatui"
license = "MIT"
readme = "README.md"
repository = "https://github.com/amjadjibon/lazytmux"
homepage = "https://github.com/amjadjibon/lazytmux"
keywords = ["tmux", "terminal", "tui", "ratatui", "workspace"]
categories = ["command-line-utilities"]

[dependencies]
# UI & Terminal
ratatui = "0.30"
# Column widths for the pane-card control strip must match what ratatui draws,
# so hit-testing uses the same width rules as rendering.
unicode-width = "0.2"
crossterm = "0.29"
ansi-to-tui = "8.0"

# Fuzzy Finding & Search
nucleo-matcher = "0.3"

# Serialization & Config
serde = { version = "1.0", features = ["derive"] }
toml = "1.1"
directories = "6.0"

# Error Handling & Utilities
anyhow = "1.0"
# default-features are off: `image-data` pulls the whole `image` crate (and
# x11 image plumbing on Linux) to support copying bitmaps, and lazytmux only
# ever calls set_text.
arboard = { version = "3.6", default-features = false }

[profile.release]
# "z" over 3: this is a UI-bound tool that spends its time waiting on tmux and
# on terminal input, not in hot loops. The only measurable compute is ANSI
# parsing, which is windowed to the visible viewport and runs at most once per
# capture. Costs ~25us per frame; saves ~320KB of binary.
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

